# 🤖 Telegram Fitness Bot

Телеграм-бот для отслеживания тренировок и рабочих весов, созданный с использованием aiogram 3.x.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка

Создайте файл `.env` в корне проекта и добавьте токен вашего бота:

```
BOT_TOKEN=your_telegram_bot_token_here
```

Токен можно получить у [@BotFather](https://t.me/BotFather) в Telegram.

### 3. Запуск

```bash
python main.py
```

## 📋 Функциональность

- ✅ Парсинг текстовой программы тренировок
- ✅ Сохранение программы в базу данных SQLite
- ✅ Интерактивное ведение тренировки с запросом весов
- ✅ Автоматическое предложение предыдущих весов
- ✅ Статистика по упражнениям
- ✅ FSM для управления состоянием тренировки

## 📝 Формат программы тренировок

Отправьте боту программу в следующем формате:

```
ПН: жим лёжа 3x10, присед 4x8; ВТ: подтягивания 3xмакс, тяга 4x10
```

Где:
- `ПН`, `ВТ`, `СР` и т.д. — дни недели
- `жим лёжа 3x10` — название упражнения, количество подходов x повторения
- Упражнения разделяются запятыми, дни — точкой с запятой

## 🎮 Команды

- `/start` — начать работу с ботом
- `/stats` — посмотреть статистику по упражнениям

## 🗂️ Структура проекта

```
workout_bot/
├── main.py              # Точка входа
├── config.py            # Конфигурация (загрузка токена)
├── database.py          # Работа с SQLite
├── parser.py            # Парсинг программы тренировок
├── handlers/            # Обработчики команд
│   ├── start.py         # Команда /start
│   ├── training.py      # Логика тренировки
│   └── stats.py         # Команда /stats
├── utils/               # Вспомогательные модули
│   ├── keyboards.py     # Клавиатуры
│   └── helpers.py       # Вспомогательные функции
├── requirements.txt     # Зависимости
└── README.md           # Документация
```

## 💾 База данных

Бот использует SQLite базу данных с тремя таблицами:

- `users` — информация о пользователях
- `programs` — сохраненные программы тренировок
- `results` — результаты тренировок (веса по подходам)

База данных создается автоматически при первом запуске.

## 📦 Зависимости

- `aiogram==3.13.1` — фреймворк для создания Telegram ботов
- `python-dotenv==1.0.0` — загрузка переменных окружения из .env файла
